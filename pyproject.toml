[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mdh"
version = "0.1.0"
description = "Minimal Markdown-to-HTML converter CLI"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
mdh = "mdh.cli:main"
